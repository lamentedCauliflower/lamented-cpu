-- ponytail: example module — delete or replace with your own.
-- Pure logic: unit-testable under busted without the engine.
local example = {}

function example.add(a, b)
  return a + b
end

return example
