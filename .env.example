# Copy to .env (gitignored) and fill in. devenv loads it via dotenv.enable.
# Credentials for the unfree Factorio client build used by `play` (withClient).
# Find them at https://factorio.com/profile -> your username + the API token.
FACTORIO_USERNAME=your-factorio-username
FACTORIO_TOKEN=your-factorio-api-token
